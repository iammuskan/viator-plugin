(function(){
	let $termParent = document.querySelector('.form-field.term-parent-wrap'),
		$termName   = document.querySelector('.form-required.term-name-wrap');

	if ( $termParent ){
		$termParent.remove();
	}
	if ( $termName ){
		$termName.remove();
	}



}());
